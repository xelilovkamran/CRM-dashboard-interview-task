{
  "name": "dashboard-task---pug",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pug ./src/views --pretty --out ./dist",
    "watch": "pug --watch --pretty --out ./dist ./src/views",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pug": "^3.0.3",
    "pug-cli": "^1.0.0-alpha6"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
